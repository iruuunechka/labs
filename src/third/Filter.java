package third;

import second.random_forest.Dataset;

import java.util.List;

/**
 * @author Irene Petrova
 */
public class Filter {

    private static double rho(Dataset dataset, int j) {
        double numer = 0;
        double sum1denom = 0;
        double sum2denom = 0;
        for (int i = 0; i < dataset.size(); ++i) {

        }
        return 0;
    }

    public static List<Integer> filter(Dataset dataset) {
        return null;
    }
}
